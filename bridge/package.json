{
  "name": "beamscope-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "UDP → WebSocket bridge for BeamScope",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.0"
  }
}
